/*
 * Speak to Yourself — AI mirror endpoint.
 *
 * Paste your deployed Cloudflare Worker URL between the quotes after you
 * follow tools/mirror-worker.js SETUP (e.g.
 * "https://mirror.your-name.workers.dev"). While this stays empty the page
 * runs fully offline: local reflection works, the AI chat degrades to a
 * gentle offline message. Nothing else needs to change.
 */
window.VH_MIRROR_URL = '';
